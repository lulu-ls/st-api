# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Bargain API
    version: 0.0.1
paths:
    /st-games/v1/activity/bargain/detail:
        post:
            tags:
                - Bargain
            description: 查询进度接口
            operationId: Bargain_GetBargain
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetBargainRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetBargainReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /st-games/v1/activity/bargain/grant:
        post:
            tags:
                - Bargain
            description: 好友助力接口
            operationId: Bargain_GrantAward
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GrantAwardRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GrantAwardReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /st-games/v1/activity/bargain/receive:
        post:
            tags:
                - Bargain
            description: 点击气泡，领取进度接口
            operationId: Bargain_GetProcess
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetProcessRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetProcessReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        BubbleItem:
            type: object
            properties:
                id:
                    type: string
                nickname:
                    type: string
                avatar:
                    type: string
        GetBargainReply:
            type: object
            properties:
                bargainId:
                    type: string
                name:
                    type: string
                image:
                    type: string
                process:
                    type: number
                    format: double
                status:
                    type: integer
                    format: enum
                historyList:
                    type: array
                    items:
                        $ref: '#/components/schemas/HistoryItem'
                bubbleList:
                    type: array
                    items:
                        $ref: '#/components/schemas/BubbleItem'
        GetBargainRequest:
            type: object
            properties:
                appId:
                    type: integer
                    format: int32
                channelId:
                    type: integer
                    format: int32
                userId:
                    type: string
        GetProcessReply:
            type: object
            properties:
                nickname:
                    type: string
                avatar:
                    type: string
                process:
                    type: string
        GetProcessRequest:
            type: object
            properties:
                appId:
                    type: integer
                    format: int32
                channelId:
                    type: integer
                    format: int32
                userId:
                    type: string
                id:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        GrantAwardItem:
            type: object
            properties:
                itemId:
                    type: integer
                    format: int32
                name:
                    type: string
                image:
                    type: string
        GrantAwardReply:
            type: object
            properties:
                list:
                    type: array
                    items:
                        $ref: '#/components/schemas/GrantAwardItem'
        GrantAwardRequest:
            type: object
            properties:
                appId:
                    type: integer
                    format: int32
                channelId:
                    type: integer
                    format: int32
                userId:
                    type: string
                bargainId:
                    type: string
        HistoryItem:
            type: object
            properties:
                id:
                    type: string
                nickname:
                    type: string
                text:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Bargain
